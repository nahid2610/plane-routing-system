# plane-routing-system
A Java-based Plane Routing System that uses Graph data structures and Dijkstra’s algorithm to find the shortest route between airports, with Linear Search for efficient data retrieval.
